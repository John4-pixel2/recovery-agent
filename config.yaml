# Example configuration for the Recovery Agent

app_name: "Recovery Agent"

server:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "INFO"

recovery_settings:
  target_dir: "/opt/recovery_agent/restored_files"
  backup_formats:
    logs: "*.log"
    db_dumps: "*.sql"